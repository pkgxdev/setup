{
  "dependencies": {
    "semver": "^7.8.0",
    "tar": "^7.5.13",
    "unzipper": "^0.12.3"
  },
  "scripts": {
    "dist": "npx esbuild ./action.js --bundle --platform=node --target=node24 --outfile=./action.js --allow-overwrite --minify --external:@aws-sdk/client-s3"
  }
}
